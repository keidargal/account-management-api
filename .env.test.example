# Copy to `.env.test` for integration tests (`npm run test:integration`).
# Use a dedicated database — not production.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/account_management_test?schema=public"
