# Copy to `.env.test` and point to a dedicated PostgreSQL database (never production).
# Create the database once, then:  DATABASE_URL="..." npx prisma migrate deploy
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/account_management_test?schema=public"
